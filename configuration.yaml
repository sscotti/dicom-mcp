# DICOM nodes configuration
nodes:
  main:
    host: "localhost"
    port: 4242
    ae_title: "ORTHANC"
    description: "Local Orthanc DICOM server (Primary)"
  
  secondary:
    host: "localhost"
    port: 4243
    ae_title: "ORTHANC2"
    description: "Local Orthanc DICOM server (Secondary)"

current_node: "main"
calling_aet: "MCPSCU"

# FHIR server configuration (optional)
fhir:
  base_url: "http://hackathon.siim.org/fhir"  # Try HTTP if HTTPS fails
  api_key: "${SIIM_API_KEY}"  # Set in .env file
  description: "SIIM Hackathon FHIR server"

# MCP Server configuration - no additional config needed
# The server will expose all DICOM tools via standard MCP protocol